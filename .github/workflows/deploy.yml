name: DevOps Demo CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  IMAGE_NAME: devops-demo
  KUBE_NAMESPACE: devops-demo

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

  build-and-deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
    - uses: actions/checkout@v4

    - name: Build Docker image
      run: |
        docker build -t $IMAGE_NAME:latest .
        docker build -t $IMAGE_NAME:${{ github.sha }} .

    - name: Save Docker image
      run: |
        docker save $IMAGE_NAME:latest > /tmp/devops-demo.tar

    - name: Upload Docker image
      uses: actions/upload-artifact@v4
      with:
        name: docker-image
        path: /tmp/devops-demo.tar
  # Note: For local demo, you'll manually pull and deploy
  # In production, this would connect to your K8s cluster
